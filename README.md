# Phd_RL_Cav_Bonetto
Cavitation model developed by Bonetto et. al.


Resumo a continuación el funcionamiento del código de Puente. El código está escrito en C++ empleando el Builder 6 de Borland. Tiene 6 archivos principales: . ecdiff.cpp: Aquí se define la ecuación diferencial temporal . FUNCTIONS.cpp: Aquí se definen funciones básicas . PSULSO.CPP: Aquí se definen los parámetros del modelo . Rkqc.cpp: Aquí se define el método numérico llamado rkqc . rk4.cpp: Aquí se define el método numérico RK4 . sbsl.cpp: Este es el archivo principal que computa la dinámica de la burbuja de cavitación

Además, hay otros archivos generados por Borland, archivos con terminación .m que parece ser de matlab y no sé qué hacen, archivos duplicados de los 6 mencionados con terminación .~CPP en lugar de .cpp, archivos .obj que se generan al compilar el código y, por último, archivos .dat que contienen los resultados/output de la evolución. También está el file PulWild.brp desde el que se ejecuta la interfaz de Borland y nos permite compilar y ejecutar el código.

El directorio en el que se encuentra el código no debe tener guión medio!!

Para compilar el código desde cmd usar bcb -B PulWid.bpr Para ejecutarlo, llamar el .exe

Para compilar archivos por separado bcc32 -c PSULSOAR.cpp

Sobre el código anterior se creó una librería de python para compilar y ejecutar el código de Puente


Below I summarize the functioning of the Puente code. The code is written in C++ using Borland Builder 6. It has six main files:

ecdiff.cpp: This file defines the time-dependent differential equation.

FUNCTIONS.cpp: This file defines basic functions.

PSULSO.cpp: This file defines the model parameters.

Rkqc.cpp: This file defines the numerical method called rkqc.

rk4.cpp: This file defines the RK4 numerical method.

sbsl.cpp: This is the main file that computes the dynamics of the cavitation bubble.

In addition, there are other files generated by Borland: files with the .m extension that appear to be from MATLAB and whose function is unknown; duplicated versions of the six files mentioned above with the extension .~CPP instead of .cpp; .obj files that are generated when compiling the code; and finally .dat files that contain the results/output of the evolution. There is also the file PulWild.brp, from which the Borland interface is launched and which allows us to compile and run the code.

The directory containing the code must not include a hyphen (-) in its name!!

To compile the code from the command line, use:
bcb -B PulWid.bpr

To run it, execute the generated .exe file.

To compile individual files, use:
bcc32 -c PSULSOAR.cpp

On top of the previous code, a Python library was created to compile and run the Puente code.